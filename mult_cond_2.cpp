#include <stdio.h>
#include <stdio.h>

int main(){
    int idade = 18;
    float altura = 175.0;
    if (idade >= 18 && idade <= 40 && altura >= 170.0)
    {
        printf("voce esta na faixa etaria e tem altura adequada\n");
    }
    else
    {
        printf("voce nao atende aos criterios\n");
    }
    return 0;
}
